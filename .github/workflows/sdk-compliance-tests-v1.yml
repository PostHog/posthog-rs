name: SDK Compliance Tests (V1 Capture)

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'compliance/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/sdk-compliance-tests-v1.yml'
  pull_request:
    branches: [main]
    paths:
      - 'src/**'
      - 'compliance/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/sdk-compliance-tests-v1.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: read
  pull-requests: write

jobs:
  test-rust-sdk-v1:
    uses: PostHog/posthog-sdk-test-harness/.github/workflows/test-sdk-action.yml@c32f4959240967f1ee89e58490fc9d3beaa1401f
    with:
      adapter-dockerfile: compliance/v1/Dockerfile
      adapter-context: .
      test-harness-version: "0.10.0"
      report-name: rust-sdk-compliance-report-v1
      concurrency: 10
